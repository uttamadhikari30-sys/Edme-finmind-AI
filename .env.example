# ─────────────────────────────────────────────────────
# FINMIND AI — Environment variables
# Copy to .env.local for local dev. Set in Vercel → Project → Settings → Environment Variables for production.
# ─────────────────────────────────────────────────────

# Supabase (public — safe to commit / expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://gdtqujtdsdmvvtnxjdks.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_FB_WD-hkoj4vLQEnLzAP8g_H5fb3ZB0

# Anthropic — Maya AI assistant. SERVER-SIDE ONLY, never expose to client.
# Get a key at https://console.anthropic.com/settings/keys
# Without this set, Maya falls back to rule-based responses (still works, just less smart).
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxx
